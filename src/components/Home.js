import Display from "./Display"

const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<>
			
			<Display/>
			
			
		</>
			
		
	)
}

export default Home
